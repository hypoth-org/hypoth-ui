{
  "$schema": "../../../../docs-core/src/schemas/component-manifest.schema.json",
  "id": "tabs",
  "name": "Tabs",
  "version": "1.0.0",
  "status": "stable",
  "description": "Tabbed interface for organizing content into selectable panels with keyboard navigation",
  "editions": ["core"],
  "accessibility": {
    "apgPattern": "tabs",
    "keyboard": ["ArrowLeft", "ArrowRight", "ArrowUp", "ArrowDown", "Home", "End", "Enter", "Space"],
    "screenReader": "Tab list uses role=tablist. Tabs use role=tab with aria-selected. Panels use role=tabpanel with aria-labelledby linking to tab. Automatic activation announces panel content on arrow key navigation.",
    "ariaPatterns": ["role=tablist", "role=tab", "role=tabpanel", "aria-selected", "aria-controls", "aria-labelledby"],
    "knownLimitations": []
  },
  "tokensUsed": [
    "color.primary",
    "color.text.default",
    "color.text.muted",
    "color.border.default",
    "spacing.md",
    "radius.md"
  ],
  "recommendedUsage": "Use to organize related content into distinct panels. Choose automatic or manual activation mode based on tab content complexity. Support horizontal or vertical orientation based on layout needs.",
  "antiPatterns": "Do not use tabs for sequential steps - use Stepper instead. Avoid too many tabs that cause wrapping or scrolling. Do not hide critical content in non-default tabs.",
  "platforms": ["wc", "react"]
}
