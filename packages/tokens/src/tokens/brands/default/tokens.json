{
  "$schema": "../../../schemas/dtcg-token.schema.json",
  "color": {
    "$type": "color",
    "$description": "Default brand color overrides (same as global)",
    "primary": {
      "$value": "#0066cc",
      "$description": "Primary brand color"
    },
    "interactive": {
      "$value": "{color.primary}",
      "$description": "Interactive elements color"
    }
  }
}
